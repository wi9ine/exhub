# 개별 주문 조회

개별 주문 내역을 조회합니다.

> 예시코드는 JavaScript, Python, JAVA에 한해서만 제공합니다.

<Callout icon="📌" theme="warning">
  #### 입력 규칙

  `uuid` 또는 `client_order_id` 중 하나 이상을 반드시 전달해야 하며, 두 값을 모두 전달한 경우 `uuid`를 기준으로 조회합니다.
</Callout>

<Callout icon="📘" theme="info">
  #### 사전 안내

  기존에는 자전거래 가능성이 있을 경우 주문이 접수되지 않았으나,
  개선 후에는 주문 접수 후 체결 단계에서 자전거래를 감지해
  충돌하는 신규 주문 수량만 취소하도록 **개선될 예정**입니다.
  자세한 내용은 [공지사항](https://apidocs.bithumb.com/changelog/%EC%97%85%EB%8D%B0%EC%9D%B4%ED%8A%B8-open-api-%EC%A3%BC%EB%AC%B8-%EA%B4%80%EB%A0%A8-%EC%9E%90%EC%A0%84%EA%B1%B0%EB%9E%98-%EB%B0%A9%EC%A7%80-%EC%8B%9C%EC%8A%A4%ED%85%9C-%EA%B3%A0%EB%8F%84%ED%99%94-%EC%95%88%EB%82%B4)을 참고하세요(업데이트 일시: 2026년 4월 2일 오후 12시 이후).

  응답 항목 "자전거래 방지 유형" `stp_type`, "취소유형" `cancel_type`, "취소 주문 고유 아이디" `canceling_uuid`가 신규 추가될 예정입니다.
</Callout>

# OpenAPI definition

```json
{
  "openapi": "3.1.0",
  "info": {
    "title": "open_api_public",
    "version": "2.1.5"
  },
  "servers": [
    {
      "url": "https://api.bithumb.com/v1"
    }
  ],
  "security": [
    {}
  ],
  "paths": {
    "/order": {
      "get": {
        "summary": "개별 주문 조회",
        "description": "개별 주문 내역을 조회합니다.",
        "operationId": "개별-주문-조회",
        "parameters": [
          {
            "name": "Authorization",
            "in": "header",
            "description": "Authorization token (JWT)",
            "required": true,
            "schema": {
              "type": "string"
            }
          },
          {
            "name": "uuid",
            "in": "query",
            "description": "주문의 고유 ID",
            "required": false,
            "schema": {
              "type": "string"
            }
          },
          {
            "in": "query",
            "name": "client_order_id",
            "schema": {
              "type": "string"
            },
            "description": "서버에서 부여하는 주문 ID(`uuid`)와 별도로 주문 생성 시 사용자가 직접 지정한 고유 ID\n\n- 허용 문자: 영문 대/소문자, 숫자, -, _\n- 길이: 1–36자"
          }
        ],
        "responses": {
          "200": {
            "description": "200",
            "content": {
              "application/json": {
                "examples": {
                  "Result": {
                    "value": "{\n  \"uuid\": \"C0101000000001799231\",\n  \"side\": \"bid\",\n  \"ord_type\": \"limit\",\n  \"price\": \"83000000\",\n  \"state\": \"done\",\n  \"market\": \"KRW-BTC\",\n  \"created_at\": \"2024-07-09T16:32:23+09:00\",\n  \"volume\": \"1\",\n  \"remaining_volume\": \"0\",\n  \"reserved_fee\": \"207500\",\n  \"remaining_fee\": \"0\",\n  \"paid_fee\": \"207500\",\n  \"locked\": \"0\",\n  \"executed_volume\": \"1\",\n  \"trades_count\": 1,\n  \"trades\": [\n    {\n      \"market\": \"KRW-BTC\",\n      \"uuid\": \"C0101000000001713006\",\n      \"price\": \"83000000\",\n      \"volume\": \"1\",\n      \"funds\": \"83000000\",\n      \"side\": \"bid\",\n      \"created_at\": \"2024-07-09T16:32:23+09:00\"\n    }\n  ]\n}"
                  }
                },
                "schema": {
                  "type": "object",
                  "properties": {
                    "uuid": {
                      "type": "string",
                      "example": "C0101000000001799231",
                      "description": "주문의 고유 ID"
                    },
                    "client_order_id": {
                      "type": "string",
                      "description": "서버에서 부여하는 주문 ID(`uuid`)와 별도로 주문 생성 시 사용자가 직접 지정한 고유 ID"
                    },
                    "side": {
                      "type": "string",
                      "example": "bid",
                      "description": "주문 종류\n- `bid`: 매수\n- `ask`: 매도",
                      "enum": [
                        "bid",
                        "ask"
                      ]
                    },
                    "ord_type": {
                      "type": "string",
                      "example": "limit",
                      "description": "주문 방식\n- `limit`: 지정가\n- `price`: 시장가(매수)\n- `market`: 시장가(매도)",
                      "enum": [
                        "limit",
                        "price",
                        "market"
                      ]
                    },
                    "price": {
                      "type": "string",
                      "example": "83000000",
                      "description": "주문 가격"
                    },
                    "state": {
                      "type": "string",
                      "example": "done",
                      "description": "주문 상태\n- `wait`: 체결 대기 \n- `watch`: 주문 대기\n- `done`: 체결 완료\n- `cancel`: 주문 취소",
                      "enum": [
                        "wait",
                        "watch",
                        "done",
                        "cancel"
                      ]
                    },
                    "market": {
                      "type": "string",
                      "example": "KRW-BTC",
                      "description": "거래 대상 페어의 고유 심볼\n\n예시) KRW-BTC",
                      "default": ""
                    },
                    "created_at": {
                      "type": "string",
                      "example": "2024-07-09T16:32:23+09:00",
                      "description": "주문 생성 시각",
                      "format": "date-time"
                    },
                    "volume": {
                      "type": "string",
                      "example": "1",
                      "description": "사용자가 입력한 주문 수량"
                    },
                    "remaining_volume": {
                      "type": "string",
                      "example": "0",
                      "description": "체결 후 남은 주문 수량"
                    },
                    "reserved_fee": {
                      "type": "string",
                      "example": "207500",
                      "description": "수수료로 예약된 금액"
                    },
                    "remaining_fee": {
                      "type": "string",
                      "example": "0",
                      "description": "남은 수수료 금액"
                    },
                    "paid_fee": {
                      "type": "string",
                      "example": "207500",
                      "description": "사용된 수수료 금액"
                    },
                    "locked": {
                      "type": "string",
                      "example": "0",
                      "description": "거래에 사용 중인 금액"
                    },
                    "executed_volume": {
                      "type": "string",
                      "example": "1",
                      "description": "체결된 수량"
                    },
                    "trades_count": {
                      "type": "integer",
                      "example": 1,
                      "description": "해당 주문의 체결 건수"
                    },
                    "trades": {
                      "type": "array",
                      "items": {
                        "type": "object",
                        "properties": {
                          "market": {
                            "type": "string",
                            "example": "KRW-BTC",
                            "description": "거래 대상 페어의 고유 심볼\n\n예시) KRW-BTC"
                          },
                          "uuid": {
                            "type": "string",
                            "example": "C0101000000001713006",
                            "description": "체결의 고유 ID"
                          },
                          "price": {
                            "type": "string",
                            "example": "83000000",
                            "description": "체결 가격"
                          },
                          "volume": {
                            "type": "string",
                            "example": "1",
                            "description": "체결 수량"
                          },
                          "funds": {
                            "type": "string",
                            "example": "83000000",
                            "description": "체결 금액"
                          },
                          "side": {
                            "type": "string",
                            "example": "bid",
                            "description": "체결 종류\n- `bid`: 매수\n- `ask`: 매도",
                            "enum": [
                              "bid",
                              "ask"
                            ]
                          },
                          "created_at": {
                            "type": "string",
                            "example": "2024-07-09T16:32:23+09:00",
                            "description": "체결 시각",
                            "format": "date-time"
                          }
                        }
                      },
                      "description": "체결 내역 "
                    }
                  },
                  "required": [
                    "uuid",
                    "side",
                    "ord_type",
                    "price",
                    "state",
                    "market",
                    "created_at",
                    "volume",
                    "remaining_volume",
                    "reserved_fee",
                    "remaining_fee",
                    "paid_fee",
                    "locked",
                    "executed_volume",
                    "trades_count",
                    "trades"
                  ]
                }
              }
            }
          },
          "400": {
            "description": "400",
            "content": {
              "application/json": {
                "examples": {
                  "Result": {
                    "value": "{\n  \"error\": {\n    \"name\": \"error name\",\n    \"message\": \"error message\"\n  }\n}"
                  }
                },
                "schema": {
                  "type": "object",
                  "properties": {
                    "error": {
                      "type": "object",
                      "properties": {
                        "name": {
                          "type": "string",
                          "example": "error name",
                          "description": "에러명"
                        },
                        "message": {
                          "type": "string",
                          "example": "error message",
                          "description": "에러 메시지"
                        }
                      }
                    }
                  },
                  "required": [
                    "error"
                  ]
                }
              }
            }
          },
          "401": {
            "description": "401",
            "content": {
              "application/json": {
                "examples": {
                  "Result": {
                    "value": "{\n  \"error\": {\n    \"name\": \"error name\",\n    \"message\": \"error message\"\n  }\n}"
                  }
                },
                "schema": {
                  "type": "object",
                  "properties": {
                    "error": {
                      "type": "object",
                      "properties": {
                        "name": {
                          "type": "string",
                          "description": "에러명"
                        },
                        "message": {
                          "type": "string",
                          "description": "에러 메시지"
                        }
                      }
                    }
                  },
                  "required": [
                    "error"
                  ]
                }
              }
            }
          },
          "404": {
            "description": "Not Found",
            "content": {
              "application/json": {
                "examples": {
                  "Result": {
                    "value": "{\n  \"error\": {\n    \"name\": \"error name\",\n    \"message\": \"error message\"\n  }\n}"
                  }
                },
                "schema": {
                  "type": "object",
                  "properties": {
                    "error": {
                      "type": "object",
                      "properties": {
                        "name": {
                          "type": "string",
                          "description": "에러명"
                        },
                        "message": {
                          "type": "string",
                          "description": "에러 메시지"
                        }
                      }
                    }
                  },
                  "required": [
                    "error"
                  ]
                }
              }
            }
          }
        },
        "deprecated": false,
        "x-readme": {
          "code-samples": [
            {
              "language": "javascript",
              "code": "  const jwt = require('jsonwebtoken');\n  const { v4: uuidv4 } = require('uuid');\n  const crypto = require('crypto');\n  const axios = require('axios')\n\n  const accessKey = '발급받은 API KEY'\n  const secretKey = '발급받은 SECRET KEY'\n  const apiUrl = 'https://api.bithumb.com'\n\n  // Set API parameters\n  // uuid 또는 client_order_id 중 하나를 사용\n  // 두 값이 모두 전달된 경우, uuid를 기준으로 조회\n  const params = {\n      uuid: 'C0295000000000100020',\n      // client_order_id: 'my-client-order-id-001'  // 필요 시 주석 해제\n  }\n\n  // 인코딩 없이 query string 생성\n  const query = Object.entries(params)\n      .map(([key, value]) => `${key}=${value}`)\n      .join('&')\n\n  // Generate access token\n  const alg = 'SHA512'\n  const hash = crypto.createHash(alg)\n  const queryHash = hash.update(query, 'utf-8').digest('hex')\n  const payload = {\n      access_key: accessKey,\n      nonce: uuidv4(),\n      timestamp: Date.now(),\n      query_hash: queryHash,\n      query_hash_alg: alg\n  }\n  const jwtToken = jwt.sign(payload, secretKey)\n  const config = {\n      headers: {\n          Authorization: `Bearer ${jwtToken}`\n      }\n  }\n\n  // Call API\n  axios.get(apiUrl + '/v1/order?' + query, config)\n      .then((response) => {\n          // handle to success\n          console.log('status: ', response.status)\n          console.log('data: ', response.data)\n      })\n      .catch((error) => {\n          // handle to fail\n          console.log(error.response.status)\n          console.log(error.response.data)\n      })"
            },
            {
              "language": "python",
              "code": " # Python 3\n  # pip3 installl pyJwt\n  import jwt\n  import uuid\n  import hashlib\n  import time\n  import requests\n\n  accessKey = '발급받은 API KEY'\n  secretKey = '발급받은 SECRET KEY'\n  apiUrl = 'https://api.bithumb.com'\n\n  # Set API parameters\n  param = dict( uuid='C0295000000000100020' )\n\n  # client_order_id (선택)\n  # param['client_order_id'] = 'my-client-order-id-001'\n\n  # Generate access token\n  query = '&'.join([f'{k}={v}' for k, v in param.items()])\n  hash = hashlib.sha512()\n  hash.update(query.encode('utf-8'))\n  query_hash = hash.hexdigest()\n  payload = {\n      'access_key': accessKey,\n      'nonce': str(uuid.uuid4()),\n      'timestamp': round(time.time() * 1000),\n      'query_hash': query_hash,\n      'query_hash_alg': 'SHA512',\n  }\n  jwt_token = jwt.encode(payload, secretKey)\n  authorization_token = 'Bearer {}'.format(jwt_token)\n  headers = {\n    'Authorization': authorization_token\n  }\n\n  try:\n      # Call API\n      response = requests.get(apiUrl + '/v1/order?' + query, headers=headers)\n      # handle to success or fail\n      print(response.status_code)\n      print(response.json())\n  except Exception as err:\n      # handle exception\n      print(err)"
            },
            {
              "language": "java",
              "code": "  package com.example.sample;\n\n  // https://mvnrepository.com/artifact/com.auth0/java-jwt\n  import com.auth0.jwt.JWT;\n  import com.auth0.jwt.algorithms.Algorithm;\n  // https://mvnrepository.com/artifact/org.apache.httpcomponents/httpclient\n  import org.apache.http.client.methods.CloseableHttpResponse;\n  import org.apache.http.client.methods.HttpGet;\n  import org.apache.http.impl.client.CloseableHttpClient;\n  import org.apache.http.impl.client.HttpClients;\n  import org.apache.http.util.EntityUtils;\n\n  import java.math.BigInteger;\n  import java.nio.charset.StandardCharsets;\n  import java.security.MessageDigest;\n  import java.security.NoSuchAlgorithmException;\n  import java.util.ArrayList;\n  import java.util.List;\n  import java.util.UUID;\n\n  public class GETArgs {\n\n      public static void main(String[] args) throws NoSuchAlgorithmException {\n          String accessKey = \"발급받은 API KEY\";\n          String secretKey = \"발급받은 SECRET KEY\";\n          String apiUrl = \"https://api.bithumb.com\";\n\n          // Set API parameters (URL 인코딩 없이 수동 조합)\n          List<String> queryParts = new ArrayList<>();\n          queryParts.add(\"uuid=C0295000000000100020\");\n\n          // client_order_id는 선택 - 필요 시 아래 주석 해제\n          // queryParts.add(\"client_order_id=my-client-order-id-001\");\n\n          String query = String.join(\"&\", queryParts);\n\n          // Generate access token\n          MessageDigest md = MessageDigest.getInstance(\"SHA-512\");\n          md.update(query.getBytes(StandardCharsets.UTF_8));\n          String queryHash = String.format(\"%0128x\", new BigInteger(1, md.digest()));\n          Algorithm algorithm = Algorithm.HMAC256(secretKey);\n          String jwtToken = JWT.create()\n                  .withClaim(\"access_key\", accessKey)\n                  .withClaim(\"nonce\", UUID.randomUUID().toString())\n                  .withClaim(\"timestamp\", System.currentTimeMillis())\n                  .withClaim(\"query_hash\", queryHash)\n                  .withClaim(\"query_hash_alg\", \"SHA512\")\n                  .sign(algorithm);\n          String authenticationToken = \"Bearer \" + jwtToken;\n\n          // Call API\n          final HttpGet httpRequest = new HttpGet(apiUrl + \"/v1/order?\" + query);\n          httpRequest.addHeader(\"Authorization\", authenticationToken);\n\n          try (CloseableHttpClient client = HttpClients.createDefault();\n               CloseableHttpResponse response = client.execute(httpRequest)) {\n              // handle to response\n              int httpStatus = response.getStatusLine().getStatusCode();\n              String responseBody = EntityUtils.toString(response.getEntity(), StandardCharsets.UTF_8);\n              System.out.println(httpStatus);\n              System.out.println(responseBody);\n          } catch (Exception e) {\n              throw new RuntimeException(e);\n          }\n      }\n  }"
            }
          ],
          "samples-languages": [
            "javascript",
            "python",
            "java"
          ]
        }
      }
    }
  },
  "x-readme": {
    "headers": [],
    "explorer-enabled": false,
    "proxy-enabled": false
  },
  "x-readme-fauxas": true
}
```